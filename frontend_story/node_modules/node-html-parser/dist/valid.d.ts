import { Options } from 'node-html-parser/dist/nodes/html';
/**
 * Parses HTML and returns a root element
 * Parse a chuck of HTML source.
 */
export default function valid(data: string, options?: Partial<Options>): boolean;
